---
import HsConfiguratorIsland from '../islands/hsConfigurator.jsx';
import { getI18nResources } from '../../../lib/i18n/resources.js';

const {
  lang = 'pl',
  viewProps = {},
  hydrate = 'load',
} = Astro.props;

const resources = await getI18nResources(lang);
---

{
  hydrate === 'only'
    ? <HsConfiguratorIsland lang={lang} resources={resources} viewProps={viewProps} client:only="react" />
    : hydrate === 'idle'
      ? <HsConfiguratorIsland lang={lang} resources={resources} viewProps={viewProps} client:idle />
      : hydrate === 'visible'
        ? <HsConfiguratorIsland lang={lang} resources={resources} viewProps={viewProps} client:visible />
        : <HsConfiguratorIsland lang={lang} resources={resources} viewProps={viewProps} client:load />
}
