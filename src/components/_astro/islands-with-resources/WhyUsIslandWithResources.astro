---
import WhyUsIsland from '../islands/whyUs.jsx';
import { getI18nResources } from '../../../lib/i18n/resources.js';

const { lang = 'pl', viewProps = {}, hydrate = 'visible' } = Astro.props;
const resources = await getI18nResources(lang);
---

{
  hydrate === 'only'
    ? <WhyUsIsland lang={lang} resources={resources} viewProps={viewProps} client:only="react" />
    : hydrate === 'idle'
      ? <WhyUsIsland lang={lang} resources={resources} viewProps={viewProps} client:idle />
      : hydrate === 'load'
        ? <WhyUsIsland lang={lang} resources={resources} viewProps={viewProps} client:load />
        : <WhyUsIsland lang={lang} resources={resources} viewProps={viewProps} client:visible />
}
